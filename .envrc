export PYTHONPATH="$PWD/src"
export WINGSPAN_CARD_WORKBOOK="$PWD/data/raw/wingspan-card-list.xlsx"
export MLFLOW_TRACKING_URI="$PWD/.mlruns"
export PREFECT_HOME="$PWD/.prefect"

if [ -d "$PWD/.venv" ]; then
  source "$PWD/.venv/bin/activate"
fi
